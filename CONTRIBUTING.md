# コントリビューション

## 開発

1. Issueで目的と利用者が観測できる完了条件を共有する。
2. 既存差分を保持し、1機能または1修正ごとに小さなコミットへ分ける。
3. ユーザー向け文言は文字列リソースへ追加し、日本語・英語を含む対応11言語を更新する。数によって文言が変わる場合は複数形リソースを使う。
4. `verification.md` の自動検証を実行する。

コミットメッセージは日本語Conventional Commits形式（例: `feat(player): シーク操作を追加`）にしてください。

## Pull Request

- 変更理由、利用者への影響、検証結果、未検証事項を記載する。
- UI変更には複数画面サイズのスクリーンショットを添付する。
- パスワード、確認コード、保存認証情報、トークン、Cookie、署名鍵を含めない。公開クライアント識別子と秘密値を区別する。
- 通信記録からテストを作る場合は[検証データの取り込み手順](docs/api-contracts.md)に従う。
