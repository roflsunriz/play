# 検証手順

## 自動検証

```powershell
.\gradlew.bat testDebugUnitTest lintDebug assembleDebug
```

- PKCE RFC 7636既知ベクトル
- PKCE verifierのURL安全性と一意性
- リピート状態遷移
- トークン更新安全時間
- Android lint
- Debug APK生成
- Gradle依存アーティファクトのSHA-256検証
- OSVオフラインデータベースによる既知脆弱性監査

## 実機検証

1. クリーンインストールでClient ID未入力時にログインボタンが無効である。
2. Dashboardに `http://127.0.0.1` を登録し、ブラウザ認証後にアプリへログイン状態が反映される。
3. アプリ強制終了・端末再起動後も、更新トークンが有効なら再ログインせず一覧が表示される。
4. プレイリスト、アルバム、楽曲が表示され、検索は各種最大10件を正しく統合する。
5. アクティブなSpotifyデバイスで再生、一時停止、次、前が反映される。
6. シークバーのタップ・ドラッグ位置が再生位置へ反映される。
7. リピートなし→コンテキスト→1曲→なし、シャッフルON/OFFが反映される。
8. 各操作でハプティクスが発生し、端末の触覚設定を無視した強制振動にならない。
9. アクティブデバイスがない場合、Spotifyを開く復旧手順が表示される。
10. 日本語・英語、LTR・RTL、狭幅・タブレット・横画面で文字切れや操作不能がない。
11. キャッシュディレクトリが128 MiBを超え続けず、古いアートワークが押し出される。

## 現在の未実施項目

Android 36の隔離AVDでCompose UIテスト3件、ライト配色、720×1280の狭幅、アラビア語RTL、縦スクロールを確認済み。物理端末でのハプティクス、Spotifyアカウントへのログイン、Premium再生、ログイン状態の端末再起動越し保持、128 MiB到達時の実キャッシュ押し出しは未実施。ログインと再生は外部通信とユーザー承認を伴うため、端末接続後にユーザー操作で実施する。
