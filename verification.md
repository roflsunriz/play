# 検証手順

## 自動検証

```powershell
.\gradlew.bat testDebugUnitTest lintDebug assembleDebug
```

- Login5 protobufのエンコード/デコード（成功・失敗・hashcashチャレンジ応答）
- HashCash SHA-1チャレンジ解決（末尾10ビット要件）
- リピート状態遷移
- トークン更新安全時間
- Android lint
- Debug APK生成
- Gradle依存アーティファクトのSHA-256検証
- OSVオフラインデータベースによる既知脆弱性監査

```powershell
.\gradlew.bat connectedDebugAndroidTest
```

- ログイン画面（ユーザー名・パスワード入力欄、空欄時ログインボタン無効）
- ライブラリ一覧と再生バー操作のタグ・アクション発火
- 楽曲キャッシュ（TrackCache）の取得・ヒット・失敗・LRU押し出し

## 実機検証

1. クリーンインストールで、ユーザー名・パスワード未入力時にログインボタンが無効である。
2. 実際のSpotifyアカウント（ユーザー名+パスワード）でLogin5認証に成功し、ライブラリが表示される。
3. アプリ強制終了・端末再起動後も、stored_credentialで再認証されログイン状態が維持される。
4. プレイリスト、アルバム、楽曲が表示され、検索は各種最大10件を正しく統合する。
5. アクティブなデバイスで再生、一時停止、次、前が反映される。
6. シークバーのタップ・ドラッグ位置が再生位置へ反映される。
7. リピートなし→コンテキスト→1曲→なし、シャッフルON/OFFが反映される。
8. 各操作でハプティクスが発生し、端末の触覚設定を無視した強制振動にならない。
9. アクティブデバイスがない場合、[REDACTED]を開く復旧手順が表示される。
10. 日本語・英語、LTR・RTL、狭幅・タブレット・横画面で文字切れや操作不能がない。
11. キャッシュディレクトリが128 MiBを超え続けず、古いアートワークが押し出される。
12. 楽曲プレビュー（preview_url）がアプリ内再生され、TrackCacheが100 MiB上限を超えず古い音源が押し出される。
13. spclient.wg.spotify.com経由でライブラリ表示・検索・再生操作が機能する（spclientのWeb APIプロキシ契約の確認）。

## 現在の未実施項目

Android 16実機（SH-R80P）で、Compose UIテスト3件とTrackCacheテスト4件、ログイン画面の描画を確認済み。実アカウントでのLogin5認証、ライブラリ表示、検索、再生操作、再起動越しのログイン保持、楽曲プレビュー再生と100 MiB押し出しは、外部通信とユーザー承認を伴うためユーザー操作で実施する。spclient.wg.spotify.comのWeb APIプロキシ動作も実アカウントで確認する必要がある。
