# セキュリティポリシー

## 報告

脆弱性は公開Issueへ詳細を書かず、リポジトリ所有者が案内する非公開連絡先またはGitHub Security Advisoryから報告してください。連絡先が未設定の場合は、秘密値や悪用手順を除いた最小限のIssueで非公開窓口を依頼してください。

## 対象

- 確認コード、ログインコンテキスト、保存認証情報の取り違えや漏えい
- パスワードやアクセストークンの漏えい
- Android Keystore保護の回避
- 任意URI・Intentの乗っ取り
- キャッシュやログからの個人情報漏えい

パスワード、確認コード、アクセストークン、保存認証情報、署名鍵をIssue、ログ、スクリーンショットへ投稿しないでください。認証応答の本文はログ出力しません。通信記録の原本はGit管理外の`captures/`へ置き、検証用に抽出した応答も個人情報の有無を確認します。

## ビルドキャッシュ

Kotlinのビルドキャッシュメタデータに関する[CVE-2026-53914](https://github.com/advisories/GHSA-r937-wjx7-w2jp)への対策として、2026-09-12に安定版[2.4.20](https://kotlinlang.org/docs/whatsnew2420.html)へ更新しました。Gradleの解決結果でも実際のKotlin Gradle Pluginが2.4.20であることを確認し、この脆弱性に対する一律の検査除外を削除しました。

GradleビルドキャッシュとKotlinインクリメンタルコンパイルは引き続き無効です。信頼できない共有・外部提供のキャッシュを読み込みません。再有効化する場合は、修正版の利用とキャッシュの出所を確認して検証します。

OSV-Scannerの例外は `gradle/osv-scanner.toml` に対象の版と理由を記録します。AGPが解決前のPOMメタデータとして要求する旧版と、実際にビルドクラスパスへ採用される修正版を区別し、実行される旧版を例外化しません。

ブラウザー認証のデバイスコード、更新トークン、署名付き音声URLも秘密情報として扱います。認証ページは許可したHTTPSホストとパスだけを開き、音声配信URLもHTTPSと配信先ホストを検証します。音声の平文やDRMキーをアプリのキャッシュへ保存しません。
