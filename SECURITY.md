# セキュリティポリシー

## 報告

脆弱性は公開Issueへ詳細を書かず、リポジトリ所有者が案内する非公開連絡先またはGitHub Security Advisoryから報告してください。連絡先が未設定の場合は、秘密値や悪用手順を除いた最小限のIssueで非公開窓口を依頼してください。

## 対象

- OAuth state／PKCE検証の回避
- トークンやClient Secretの漏えい
- Android Keystore保護の回避
- 任意URI・Intent・ループバックコールバックの乗っ取り
- キャッシュやログからの個人情報漏えい

SpotifyのClient Secret、アクセストークン、更新トークン、署名鍵をIssue、ログ、スクリーンショットへ投稿しないでください。

## ビルドキャッシュ

Kotlin 2.4.10には、信頼できないビルドキャッシュメタデータを読み込む場合のローカルコード実行問題（CVE-2026-53914）が報告されています。安定版の修正版が公開されるまでGradleビルドキャッシュとKotlinインクリメンタルコンパイルを無効化し、共有・外部提供されたキャッシュを読み込みません。修正版が安定公開されたらKotlinを更新してこの制限を再評価します。

OSV-Scannerの例外は `gradle/osv-scanner.toml` に期限と理由を記録します。AGPが解決前のPOMメタデータとして要求する旧版と、実際にビルドクラスパスへ採用される修正版を区別し、実行される旧版を例外化しません。
