# 変更履歴

このプロジェクトの重要な変更は、[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従って記録します。

## [Unreleased]

### Added

- プレイリストの中身に並び替えを追加した。タイトル・作成者・アルバム・追加日から選び、初期値は追加日の新しい順である。
- アルバムの中身に並び替えを追加した。アルバム順・タイトル・再生数から選び、初期値はアルバム順である。各指定値は保存し、再起動後も再現する。
- 再生設定にシーク時クロスフェードを追加した。有効にすると同曲内のシークを予備デコーダーで重ね、旧位置から新位置へゲイン合計1のままつなぐ。初期値は無効である。

### Fixed

- シークや曲切り替えでライセンス取得が混雑（HTTP 429）して再生が止まる問題を修正した。同じ要求・認証のままRetry-Afterに従って再試行し、要求全体を4秒以上空け、切り替え先が取れない間は元の曲を止めず切り替えの断念を通知する。
- 一覧から別の曲を選んだ切り替えで、先に無音になってからクロスフェードが始まる問題を修正した。選曲置き換えに続く重複した再生要求が両デコーダーを消音してフェードを作り直していたため、重なりが有効な間は音量へ触れないようにした。
- 再生していない曲の歌詞を見せないよう、歌詞の表示場所を楽曲詳細から拡大プレイヤーの再生中の曲へ移動した。
- 新規インストール直後や初回ログイン直後に10秒以降の再生がDRMライセンス取得失敗になる問題を修正した。再生派生認証の初回取得がDRM要求の背後で間に合わないことが原因のため、ログイン直後に事前取得し、再生開始前にも取得待ちしてから再生する。認証失敗は通信不良へ潰さず原因を保持する。

## [0.3.0] - 2026-09-15

### Added

- 再生開始・一時停止・停止・曲送りを滑らかにつなぐため、個別に1〜12秒を設定するフェードイン、フェードアウト、クロスフェードと拡大プレイヤーの停止ボタンを追加した。ピーク音量ノーマライザーと、サービスの対象判定・cuepointを使うAutomixも再生設定から切り替えられる。
- 曲を聴きながら歌詞を追えるよう、配信元の同期歌詞を詳細画面へ追加した。手動スクロールで追従を解除し、「同期する」で復帰、時刻付きの行を押してその位置から再生できる。
- アーティストを起点に曲や作品を探せるよう、フォロー、人気曲、作品種別別ディスコグラフィー、ソングラジオ、リスナー数と紹介文、参加作品、関連プレイリスト、おすすめアーティストを表示し、既存の再生・保存・詳細経路へ接続した。個別に取得できない関連項目は件数と再試行を示し、他の情報を保持する。

- 保存曲へプレイリスト一覧から直接移動できるよう、先頭に「お気に入りの曲」を追加した。
- 曲・アルバムの保存状態をログイン先へ反映できるよう、お気に入りの追加・削除と、検索できるプレイリスト選択画面を追加した。登録済みをチェックで表示し、アルバム追加時は不足する曲だけを登録する。
- 曲から関連する音楽へ移動できるよう、ソングラジオとアーティストへのメニューを追加した。グローバル検索を7カテゴリで絞り込めるようにし、番組・ジャンルの詳細にも接続した。
- 就寝時に再生と画面を止められるよう、経過時間または時刻を指定するスリープタイマーを追加した。設定した秒数でフェード後に停止して消灯し、再生が終わっている場合は消灯だけを行う。必要なAndroid権限の設定も画面から案内する。

### Fixed

- 「トリッカル」の「すべて」「プレイリスト」検索で、所有者IDのエンコード表記を別ユーザーと誤判定する問題を修正した。
- 「インターネット」の「すべて」「ジャンル＆気分」検索に含まれる「お気に入りの曲」を正しく扱い、検索結果から保存曲の一覧を開けるようにした。
- 検索入力や画面移動によるキャンセルで更新済みの認証情報を失わないよう、認証更新の開始後は保存まで完了させるようにした。すでに失効した保存認証は一般の通信エラーへ埋もれず、ログイン操作を案内する。

- 一部の楽曲やアルバムが再生開始時に失敗する問題を修正した。サービスが再生可能な別版を返す場合、元の曲との対応を確認して音声を取得する。
- CIがSDK準備中に停止しないよう、取得できなくなった旧`tools`パッケージを要求せず、必要な`platform-tools`を明示するようにした。

### Changed

- 再生通信の調査結果を安全に残せるよう、既存の通信記録ツールに再生関連の対象を追加し、ライセンス本文を保存せずサイズとステータスを記録するようにした。認証通信の除外と原通信の維持を自動検査する。
- 終了した解析の試作が通常ビルドへ混ざらないよう、未コミットの診断コードと設定差分を復元可能なローカル資料へ整理し、復元手順を記録した。

## [0.2.0] - 2026-09-13

### Changed

- 一覧で作品を見分けやすくするため、汎用の提供元表記を作成者・説明・曲数・アルバム名・長さなどの実メタデータへ置き換えた。プレイリストの編集と削除は再生の横へアイコンでまとめ、外部アプリへ開く操作とログイン中のログインメニューを削除した。
- タブを切り替えるたびに待たずに済むよう、3種類の一覧を先読みして保持するようにした。表示範囲と近くの作品詳細も上限付きで先読みし、同じ取得要求を共有する。
- 再生位置の終端到達をフル再生成功とした検証記録を訂正し、実際の音声を確認する検査へ改めた。

- 再生用のペアリング操作をなくすため、通常のブラウザーログインと同一端末への自動復帰へ変更した。ログイン情報は暗号化して保存し、自動更新する。旧保存認証から操作なしで移行する試みはサーバーに拒否されたため、成功扱いにしない。
- 保存曲・アルバムの名前、画像、詳細と検索結果を取得するため、Windows参照版のカタログ要求へ接続した。認証方式に応じて要求ヘッダーを分け、端末SDKトークンとの混在を避ける。
- 端末内フル再生の実装として、プレビュー再生と仮の遠隔操作をMedia3の再生サービス・Android標準DRMへ置き換えた。暗号化音声のキャッシュは512 MiBを上限に押し出す。
- 再生・画像表示の停止不具合を避けるため、[Media3 1.11.1](https://github.com/androidx/media/releases/tag/1.11.1)と[Coil 3.6.2](https://coil-kt.github.io/coil/changelog/#362-september-4-2026)へ更新した。依存の検証値はGradleで再生成し、全OS用のビルドツールも解決した。

### Added

- 好みに合わせてPlay内の音を調整できるよう、メニューにプリアンプ（−10〜+10dB）と30バンドEQ（25Hz〜20kHz、各−12〜+12dB）を追加した。7プリセットと名前付き保存5スロットを備え、適用・上書き・名前変更・削除と再起動後の復元に対応する。調整は再生中にも反映される。
- 起動直後からプレイリストを選べるよう、一覧と作成者の表示名を端末へ保存するキャッシュを追加した。裏で原本一覧と照合し、追加・削除・メタデータ変更だけを保存内容へ反映する。通信失敗や不完全な応答で保存一覧を消さず、ログアウト時は該当アカウントの保存内容を取り除く。
- 保存内容を探しやすくするため、プレイリスト・アルバム・楽曲に共通のメタデータ絞り込みと並び替え、検索前のおすすめと入力中の候補・検索結果を追加した。検索語と並び順はタブごとに保持する。
- 再生中の曲を大きな画像で確認できるよう、ミニ再生画面から開くアルバムアート付きコントローラーを追加した。縦画面・横画面で戻る、シーク、再生操作を使える。
- Play内でプレイリストを管理できるよう、非公開での作成、名前・説明・画像の編集、ライブラリからの削除を追加した。保存や画像の読み込みに失敗しても入力を保持し、再試行できるようにした。
- プレイリスト変更が実際に保存されることを確認するため、画像の取得・内容照合、収録曲、削除、既存一覧の維持まで検証する実アカウント用テストを追加した。

- 再生位置だけ進む無音を見逃さないよう、実音源の16〜30秒区間に復号後の音声が含まれるかを検査する、明示実行の実機テストを追加した。音声や鍵を保存せず、音声レベルだけを集計する。

- ライブラリをAndroidから参照できるように、ブラウザー認証、暗号化セッション保持、ライブラリ表示、横断検索、作品詳細、再生操作を追加した。
- 収録曲を選んで再生したり曲からアルバムへ移動できるよう、作品詳細と戻る操作を追加した。同一曲が複数あるプレイリストでも選択した位置を保持する。
- 再生・前後移動・シーク・シャッフル・リピートの操作結果を触覚でも確認できるように、主要操作へハプティクスを追加した。
- 画像再表示を滑らかにしながら容量を制御できるように、128 MiB上限のLRUアートワークキャッシュを追加した。
- 9.1.78.2218の認証（Login5）、ストリーム（GetMediaManifest/RequestSegmentData）、オフライン、キャッシュ、ハプティクス構成を調べ、公開契約で再現できる設計判断を記録した。
- 日本語を含む主要11言語で操作できるようにUI文言を翻訳し、アラビア語とウルドゥー語のRTLレイアウトへ対応した。

### Fixed

- 先読みによるスクロールの引っかかりを減らすため、詳細の取得開始をスクロール停止後に限定し、待機範囲と同時取得数を抑えた。認証情報の読み取りを画面スレッドの外へ移し、先読み完了ごとの一覧の再構築をなくした。
- 起動後の他タブの先読みと、取得済み詳細を開く操作でも画面を待たせないよう、入口に残っていた同期の認証確認を非同期化した。取得済みタブの内容を重ねて公開する処理も省いた。
- プレイリストの作成者を識別しやすくするため、アカウントIDの直接表示をプロフィールの表示名へ置き換えた。同じ作成者の取得結果を共有・キャッシュし、所有権や編集権限には従来のIDを使う。
- 検索結果に取得不能な項目が混ざると全体が失敗する問題を修正し、利用できる項目を表示するようにした。入力の変更で古い検索結果が新しい結果を上書きしないようにした。
- 約10秒以降に曲が無音になる問題を修正した。保存済みログインから再生用の認証をPlay内部で取得・更新し、対応する端末認証と合わせて標準DRMへ渡すようにした。ペアリング、SDKアプリの新規登録、外部プレイヤーへの移動は追加していない。
- 低い横画面でキーボードに保存ボタンが隠れないよう、画面サイズ変更とIMEの余白処理をそろえ、必要な場合に編集画面の見出しを畳むようにした。
- 端末認証のAndroid情報で、OSバージョンと画面寸法の型・配置を訂正し、固定の機種情報を実行端末の値へ置き換えた。

- 曲を選んだときに認証ページを勝手に開かないようにし、ログイン開始を通常のログイン操作に限定した。
- ログイン結果の取り違えを防ぐため、PKCEとstate、コールバックのパス・重複パラメーターを検証し、キャンセル時は待機用リスナーを解放するようにした。
- ブラウザーでのログイン中にOSがアプリを凍結して結果を受け取れなくなる問題を防ぐため、ログイン待機だけを前景サービスで維持し、終了時に停止するようにした。
- 入れ替わった更新トークンをプロセス終了時に失わないよう、暗号化した認証情報の書き込み完了を確認してから処理を返すようにした。不正な更新応答を保存せず、保存に失敗した移行をデータ破損として削除しないようにした。

- 非同期の一覧・詳細・再生状態の取得が確認コード画面や新しい画面状態を巻き戻さないよう、結果を取得してから最新状態へ反映するようにした。
- ログアウトやアカウント切り替えの直前に始まった認証更新が、以前のアカウントを復活させないようにした。
- 再生サービスがDRM初期化時に停止しないよう設定値を修正し、曲の終端からの再開と接続中の停止にも対応した。
- シークのドラッグ中に再生位置の通知が来てもつまみが戻らないようにし、指を離した時点の位置へ移動するようにした。

- 無題の保存プレイリスト1件が原因で一覧全体が失敗しないよう、取得済みの属性を使って表示し、空URIとタイトルの位置ずれを防いだ。
- 横画面でも一覧のタイトルを読んで項目を押せるよう、ウィンドウの幅と高さに応じて再生操作を1行へまとめた。
- 期限切れの保存セッションでも確認コードから復帰できるよう、コード画面を接続した。キャンセル時は保存情報を保持し、更新応答が保存認証情報を省略しても従来の値を維持する。
- 不要な認証要求と計算を抑えるため、クライアントトークンを期限内で共有し、致命的なログインエラーでは即停止、計算チャレンジでは時間上限と中断を適用した。
- メタデータ要求が実際のアカウント条件に合うよう、認証済み属性から国・契約種別を取得し、参照アプリで確認した要求ヘッダーを反映した。実機で残る取得失敗は検証記録へ明記した。
- ログイン後の保存一覧を正しく読めるよう、プレイリストの改訂番号、コレクションの追加時刻、メタデータ要求と曲の画像・長さの解釈を修正した。
- 保存件数が多い場合も続きを取得できるよう、プレイリストとコレクションのページ送りを実装し、削除済み項目を除外した。
- 取得失敗を空の一覧と取り違えないよう、メタデータの通信・提供元エラーを伝播し、タブ切り替え時の旧リクエストを取り消すようにした。
- 起動時の認証更新が正しい経路を使うよう、保存認証情報はv3へ、パスワード認証はv4へ送るよう修正した。
- 未知のprotobufフィールドで読み取り位置がずれる問題を修正し、破損・切断された応答を検出するようにした。検索の曲・アルバム・プレイリストも個別の形式で読むよう修正した。
- LinuxのCIでGradleを起動できるよう実行属性を設定し、旧ランタイムの警告を解消するため各アクションを公式確認済みのNode 24対応版へ更新した。
- ライト配色の文字が暗いウィンドウ背景へ重なって読みにくくなる問題を防ぐため、Composeルートへ配色に応じたSurface背景を追加した。
- 狭幅画面をスクロールした際に本文がシステムバーと重ならないように、初期設定画面へ安全領域の余白を追加した。

### Security

- 修正済みのKotlin安定版2.4.20へ更新し、ビルドキャッシュの既知脆弱性に対する一律の監査除外を削除した。検証メタデータを再生成し、Windows・Linux・macOS用ビルドツールの確認も再現可能にした。
- 認証情報が端末ログへ残らないよう、認証応答とクライアントトークン応答の本文出力を削除した。
- 前回の検索応答を継続検証に使えるよう、周辺制御情報を除いたfixtureと取り込みスクリプト、出典の記録を追加した。
- ビルド時依存の既知脆弱性を避けるため、Commons Lang、jose4j、Bouncy Castle、JDOMを修正版へ固定した。信頼できないキャッシュを読み込まないよう、共有ビルドキャッシュとインクリメンタルコンパイルの無効設定を維持する。
